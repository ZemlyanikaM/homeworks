#Task 1
# подняла еще одну виртуалку с убунту. В доступе две виртуалки для второго задания. Понятно что для него во вновь созданной ОС нужно будет установить docker
#Думаю команды установки докера ни к чему тут, все итак понятно.

#Task2
#Инициализирую кластер на первой машине:

sudo docker swarm init
#в резульате получаю токен  да и в целом команду  для присоединения других нод к кластеру.

#перехожу в ssh-сеанс второй машины и присоединяю к кластеру:
sudo docker swarm join --token SWMTKN-1-3sd4xzkf5715x9t30dw7mhvcvweradtrw1upt1x4ja0nmxgd1s-a6xl7vv7gghgnhpid98g7v3ox 192.168.122.141:2377

#перехожу снова в ssh-сеанс первой машины, которая является менеджером (лидером) в кластере и повышаю роль второй ноды до лидера командой:
sudo docker node promote ubnt2

#Проверяю:
sudo docker node ls


#далее рамечаю labeles на обоих нодах командами:
sudo docker node update --label-add TAG=stage ubnt2
sudo docker node update --label-add TAG=prod ubnt

#далее проверяю что lables разметились (а куда бы они делись)
sudo docker node inspect ubnt
sudo docker node inspect ubnt2

#смотрю в Spec: Lables.

#скрин Task2 - на нем уже финальный результат, видны обе ноды, виден вывод inspect для одной из нод. Скрин со всеми командами сделать не удалось, консоль была заполнена стравочной инфой, обращалсь к справке docker.
# да и чего там скринить команды, итак все видно: обе ноды созданы, роли розданы, метки развешаны - (метка второй ноды видна на скрине Task2-2) 




